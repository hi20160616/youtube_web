package data

import (
	"testing"
)

func TestGetAllActivitiesVideos(t *testing.T) {
	// cr := &ChannelsRepo{PublishAfter: -1 * 24 * 60}
	// cr, err := cr.ReadChannels()
	// if err != nil {
	//         t.Error(err)
	// }
	// cr, err = cr.GetAllActivitiesVideos()
	// if err != nil {
	//         t.Error(err)
	// }
	// for _, v := range cr.VideoLists {
	//         next := v.NextPageToken
	//         log.Println(next)
	//         for _, vv := range v.Items {
	//                 log.Println(vv.Snippet.Title)
	//         }
	// }
}
